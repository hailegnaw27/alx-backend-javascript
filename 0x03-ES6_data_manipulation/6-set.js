// Define the function setFromArray
const setFromArray = (array) => {
  // Convert the array to a Set using the Set constructor
  return new Set(array);
};

// Export the function as the default export
export default setFromArray;

